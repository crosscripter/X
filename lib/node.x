# NodeJS Interop

# Allows requiring NodeJS modules
require = 'require(pop())' js 

# Allows calls to NodeJS module functions
call = swap 'pop()[pop()]()' js

# Setup alias for console.log
console.log = .