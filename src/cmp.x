< = lt
lt = `pushr((l, r) => l < r)`

> = gt
gt = `pushr((l, r) => l > r)`

<= = lte 
lte = `pushr((l, r) => l <= r)`

>= = gte
gte = `pushr((l, r) => l >= r)`

== = eq
eq = `pushr((l, r) => l === r)`

!= = neq
neq = `pushr((l, r) => l !== r)`